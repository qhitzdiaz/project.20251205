## Property Management Backend (scaffold)

- Placeholder for API service. Choose your stack (e.g., Flask/FastAPI/Node).
- Define its own database (separate from other apps) and environment variables.
- Add a dedicated docker-compose entry when ready.
