# Frontend API Configuration (OPTIONAL)
#
# By default, the frontend uses window.location.origin for API calls.
# This allows it to work through the reverse proxy on any network.
#
# If you need to specify explicit API URLs (e.g., for direct access),
# uncomment the variables below and update with your actual IPs/domains.
#
# NOTE: These variables are baked into the build at compile time.
# If you change them, you must rebuild the frontend with: npm run build

# Example configuration for direct backend access on network 192.168.2.98
# REACT_APP_AUTH_API_URL=http://192.168.2.98:50010/api
# REACT_APP_MEDIA_API_URL=http://192.168.2.98:50011/api
# REACT_APP_CLOUD_API_URL=http://192.168.2.98:50012/api
# REACT_APP_PROPERTY_API_URL=http://192.168.2.98:50050/api
# REACT_APP_SUPPLY_API_URL=http://192.168.2.98:50070
# REACT_APP_SERBISYO_API_URL=http://192.168.2.98:50090/api
# REACT_APP_DENTAL_API_URL=http://192.168.2.98:50013/api

# How to use:
# 1. Copy this file: cp frontend/.env.example frontend/.env
# 2. Edit frontend/.env with your actual URLs
# 3. Rebuild: npm run build && docker compose up -d --build frontend
